# Test configuration : 
#    /usr/share/logstash/bin/logstash -f logstashpipeline.conf --config.test_and_exit
#
# Run with automatic reload pipeline :
#    /usr/share/logstash/bin/logstash -f logstashpipeline.conf --config.reload.automatic
#
# Transmitted data wist be a sequence of json encoded records seperated by a new line. This
# encoding implies that the json record contains no \n and no \r. 
#

input {
  tcp {
    port => 3001
    codec => json_lines
  }
}
# The filter part of this file is commented out to indicate that it is
# optional.
# filter {
#
# }
output {
    stdout { codec => rubydebug }
}